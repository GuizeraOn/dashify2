---
phase: 08-aposentadoria-do-google-sheets-limpeza-e-validacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - README.md
  - hooks/useSalesWatcher.ts
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03
must_haves:
  truths:
    - lib/sheets.ts e check-sheets.js foram removidos do projeto.
    - googleapis foi desinstalada do package.json e node_modules.
    - .env.example e README.md documentam o webhook da Perfect Pay e não contêm mais credenciais do Google Sheets.
    - npm run build executa com sucesso absoluto gerando os bundles de produção.
    - Webhook e rota sales-pulse operam perfeitamente integrados ao Supabase.
  artifacts:
    - package.json
    - .env.example
    - README.md
  key_links:
    - app/api/webhooks/perfectpay/route.ts recebe vendas em tempo real
    - lib/sales-service.ts fornece dados para todo o dashboard sem sheets
---

<objective>
Aposentar definitivamente o Google Sheets: deletar arquivos legados (`lib/sheets.ts`, `check-sheets.js`), desinstalar a biblioteca `googleapis`, atualizar a documentação e variáveis de ambiente, e validar a aplicação com build de produção e teste funcional.

Purpose: Finalizar o Milestone v2.0 com uma base de código limpa, enxuta, autônoma e 100% desacoplada do Google Sheets.
Output: Repositório sem dependências do Google, documentação atualizada e build de produção validado.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-aposentadoria-do-google-sheets-limpeza-e-validacao/08-CONTEXT.md
@.planning/phases/08-aposentadoria-do-google-sheets-limpeza-e-validacao/08-RESEARCH.md
@package.json
@.env.example
@README.md
@hooks/useSalesWatcher.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover arquivos legados e desinstalar googleapis</name>
  <files>lib/sheets.ts, check-sheets.js, package.json</files>
  <read_first>package.json</read_first>
  <action>
1. Deletar `lib/sheets.ts`.
2. Deletar `check-sheets.js`.
3. Executar `npm uninstall googleapis` para remover a dependência de `package.json` e `package-lock.json`.
  </action>
  <verify>
    <automated>node -e "if (require('fs').existsSync('lib/sheets.ts') || require('fs').existsSync('check-sheets.js')) process.exit(1);"</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Atualizar documentação e variáveis de ambiente</name>
  <files>.env.example, README.md, hooks/useSalesWatcher.ts</files>
  <read_first>.env.example, README.md, hooks/useSalesWatcher.ts</read_first>
  <action>
1. Em `.env.example`:
   - Remover as variáveis `GOOGLE_SERVICE_ACCOUNT_JSON` e `SPREADSHEET_ID`.
   - Documentar `PERFECTPAY_WEBHOOK_TOKEN` como obrigatória para validar postbacks da Perfect Pay.
2. Em `README.md`:
   - Atualizar a descrição do projeto para refletir a arquitetura v2.0 (Webhook Nativo Perfect Pay + Supabase).
   - Atualizar a tabela de variáveis de ambiente.
   - Adicionar seção instruindo como cadastrar a URL de Webhook no painel da Perfect Pay (`https://seu-dominio.vercel.app/api/webhooks/perfectpay`) com o token.
   - Atualizar lista de scripts SQL para incluir `supabase_vendas.sql`.
3. Em `hooks/useSalesWatcher.ts`:
   - Atualizar comentários e mensagens de erro substituindo referências a "planilha" por "base de vendas".
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Validar build de produção e teste funcional</name>
  <files>package.json</files>
  <read_first>package.json</read_first>
  <action>
1. Executar `npm run build` para garantir que o Next.js compila todas as rotas e componentes sem nenhum erro.
2. Executar `node scripts/test-webhook.mjs` para revalidar todos os 27 testes do webhook.
3. Executar `node --env-file=.env.local scripts/test-api-routes.mjs` para revalidar as rotas analíticas.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>

<verification>
- `npm run build` completa com código 0.
- `googleapis` não consta em `package.json`.
- `lib/sheets.ts` não existe.
- Todos os testes unitários e de integração passam com 100% de sucesso.
</verification>

<success_criteria>
- Requisitos CLEAN-01, CLEAN-02 e CLEAN-03 cumpridos.
- Milestone v2.0 totalmente operacional e testado.
</success_criteria>
