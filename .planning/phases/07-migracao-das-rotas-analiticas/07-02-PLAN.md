---
phase: 07-migracao-das-rotas-analiticas
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - app/api/campaigns/route.ts
  - app/api/reports/route.ts
  - app/api/products/route.ts
  - scripts/test-api-routes.mjs
autonomous: true
requirements:
  - API-03
  - API-04
  - API-05
must_haves:
  truths:
    - /api/campaigns cruza as vendas da tabela sales com os insights do Meta Ads sem consultar o Google Sheets.
    - /api/reports calcula heatmaps, etapas de funil e ranking de faturamento por país diretamente da tabela sales.
    - /api/products calcula métricas agregadas por produto utilizando dados e product_codes da tabela sales e app_settings.
    - Todas as rotas analíticas respondem em menos de 500ms com dados íntegros.
  artifacts:
    - app/api/campaigns/route.ts
    - app/api/reports/route.ts
    - app/api/products/route.ts
    - scripts/test-api-routes.mjs
  key_links:
    - app/api/campaigns/route.ts consome fetchSales de lib/sales-service.ts
    - app/api/reports/route.ts consome fetchSales de lib/sales-service.ts
    - app/api/products/route.ts consome fetchSales de lib/sales-service.ts
---

<objective>
Migrar as rotas analíticas especializadas (`/api/campaigns`, `/api/reports` e `/api/products`) para consumirem 100% o Supabase e validar a integridade e tempo de resposta de toda a API analítica do Dashify.

Purpose: Completar a transição de leitura de dados de todas as telas do sistema para o Supabase, eliminando completamente as chamadas ao Sheets API antes da fase de limpeza e desinstalação.
Output: Rotas `/api/campaigns`, `/api/reports` e `/api/products` refatoradas e script de teste funcional `scripts/test-api-routes.mjs`.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-migracao-das-rotas-analiticas/07-CONTEXT.md
@.planning/phases/07-migracao-das-rotas-analiticas/07-RESEARCH.md
@lib/sales-service.ts
@app/api/campaigns/route.ts
@app/api/reports/route.ts
@app/api/products/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar /api/campaigns para consumir Supabase</name>
  <files>app/api/campaigns/route.ts</files>
  <read_first>app/api/campaigns/route.ts, lib/sales-service.ts</read_first>
  <action>
1. Remover imports de `@/lib/sheets` e `@/lib/parsers/vendas`.
2. Importar `fetchSales` de `@/lib/sales-service`.
3. Substituir a consulta do Sheets por `fetchSales({ dateStart, dateEnd })`.
4. Manter todo o algoritmo de indexação hierárquica (`campaignIndex`, `adsetIndex`, `adIndex`) e correspondência fuzzy com `utm_campaign`, `utm_term` e `utm_content`.
5. Preservar o formato de resposta `{ items, unsplitSpend, unsplitRows, hasVideoData, hasCommentData }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Refatorar /api/reports para consumir Supabase</name>
  <files>app/api/reports/route.ts</files>
  <read_first>app/api/reports/route.ts, lib/sales-service.ts</read_first>
  <action>
1. Remover imports de `@/lib/sheets` e `@/lib/parsers/vendas`.
2. Importar `fetchSales` de `@/lib/sales-service`.
3. Buscar dados via `fetchSales({ dateStart, dateEnd, products })`.
4. Manter os cálculos de `heatmapData`, `funnelData` e `countryRanking`.
5. Retornar JSON idêntico ao contrato consumido pelo frontend.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Refatorar /api/products para consumir Supabase</name>
  <files>app/api/products/route.ts</files>
  <read_first>app/api/products/route.ts, lib/sales-service.ts</read_first>
  <action>
1. Remover imports de `@/lib/sheets` e `@/lib/parsers/vendas`.
2. Remover varredura de `Log_Webhooks!C:C` via Google Sheets API (os códigos e garantias já estão consolidados em `app_settings` e na coluna `product_code` da tabela `sales`).
3. Importar `fetchSales` de `@/lib/sales-service`.
4. Buscar vendas do período via `fetchSales({ dateStart, dateEnd })`.
5. Calcular `ProductTotals` agregando tentativas, vendas aprovadas, faturamento líquido e bruto, reembolsos, países e meios de pagamento por produto.
6. Retornar `{ products: ProductTotals[] }`.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Criar script de teste de integração e validar performance</name>
  <files>scripts/test-api-routes.mjs</files>
  <read_first>scripts/migrate-sheets-to-supabase.mjs</read_first>
  <action>
Criar `scripts/test-api-routes.mjs` para testar diretamente a camada de serviço e funções das rotas:
1. Testa `fetchSalesPulse()` garantindo resposta com `rows` e `signature` em < 50ms.
2. Testa `fetchSales()` com e sem filtros de data, produto e país.
3. Valida que nenhum arquivo em `app/api/` ainda importa de `@/lib/sheets`.
4. Executa verificação completa de tipos (`npx tsc --noEmit`).
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/test-api-routes.mjs</automated>
  </verify>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` executa com zero erros.
- `node --env-file=.env.local scripts/test-api-routes.mjs` roda com 100% de sucesso.
- Nenhuma rota em `app/api/` faz import de `@/lib/sheets`.
</verification>

<success_criteria>
- Requisitos API-03, API-04 e API-05 cumpridos.
- 100% das rotas do dashboard migradas para o Supabase.
</success_criteria>
